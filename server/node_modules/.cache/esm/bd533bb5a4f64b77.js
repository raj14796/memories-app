let mongoose;_a4f‍.x([["default",()=>_a4f‍.o]]);_a4f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const postSchema = mongoose.Schema({
    title : String,
    message : String,
    creator : String,
    tags : [String],
    selectedFile : String,
    likeCount : {
        type : Number,
        default : 0
    },
    createdAt : {
        type : Date,
        default : new Date()
    },
});

const postMessage = mongoose.model("PostMessage",postSchema);

_a4f‍.d(postMessage);